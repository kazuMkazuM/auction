<h1>出品商品一覧</h1>
<ul>
  <% @items.each do |item| %>
    <li>  <%= link_to item.name,item %></li>
  <% end %>
</ul>
<!-- XXXのぶんにXXX_pathのしてをして、新規登録(newアクション)が呼ばれるように修正 -->
<%= link_to "出品する", new_item_path  %>
